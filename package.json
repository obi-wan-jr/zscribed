{
  "name": "dscribe",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node server/index.js",
    "start": "NODE_ENV=production node server/index.js",
    "build:css": "tailwindcss -i public/styles.css -o public/styles.build.css --minify",
    "postinstall": "echo 'Ensure ffmpeg is installed on the host'",
    "test": "node --test"
  },
  "dependencies": {
    "busboy": "^1.6.0",
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "mime": "^3.0.0",
    "morgan": "^1.10.0",
    "nanostores": "^0.9.5",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.14"
  }
}
