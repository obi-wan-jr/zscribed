{
  "name": "dscribe",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node server/index.js",
    "start": "NODE_ENV=production node server/index.js",
    "build:css": "tailwindcss -i public/styles.css -o public/styles.build.css --minify",
    "test": "node --test"
  },
  "dependencies": {
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "morgan": "^1.10.0"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.14"
  }
}
